import { Resume } from "@/lib/types";

export default function WorkExperience({resume, setResume}: {resume: Resume, setResume: (resume: Resume) => void}) {
    return (
        <>
        
        </>
    );
}